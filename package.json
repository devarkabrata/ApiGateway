{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Config-driven Express API Gateway — proxies requests to multiple backends based on route prefix",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^4.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
